// Variáveis

let idade = 33; // Criando a primeira variável

console.log(idade);

// Tipos de variáveis

idade = 33;                     // Number

let numero = 30;                // Number
let altura = 1.85;              // Number
let nome = 'Luiz Miguel';       // String (Texto)
let estudando = true;           // Booleano (True ou False)

console.log(numero, typeof numero);
console.log(idade, typeof idade);
console.log(altura, typeof altura);
console.log(nome, typeof nome);
console.log(estudando, typeof estudando);

let outra;

console.log(outra);

let nula = null;

console.log(nula);

var variavel = 30;              // Não utilizar o var

const constante = 100;

console.log(constante);